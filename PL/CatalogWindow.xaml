<Window x:Class="PL.CatalogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="HELP" Height="450" Width="800">

    <Grid Background="#FFDEDBD2">
        <Grid.RowDefinitions>
            <RowDefinition Height="101*"/>
            <RowDefinition Height="116*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="ProductList" ItemsSource="{Binding Products}" Margin="0,78,0,10" Background="#FFDEDBD2" Grid.RowSpan="2"
               CanUserDeleteRows="False" 
                  CanUserResizeRows="False"
                  AreRowDetailsFrozen="True"
                  CanUserAddRows="False"
                   IsReadOnly="True"
                  MouseDoubleClick="ProductList_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="add to cart"
                                    MinWidth="10">

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                    Height="30" Width="30"
                                    HorizontalAlignment="Center"
                                    Background="#B0C4B1"
                                    BorderBrush="#FF4A5759"
                                    IsEnabled="True"
                                    MouseDoubleClick="Add_To_Cart_click" >
                                <materialDesign:PackIcon Kind="CartAdd"  BorderBrush="#FF4A5759"/>
                            </Button>
                        </DataTemplate>

                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="choose amount"
                                    MinWidth="10">

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:Name="Amount">
                            <TextBox Name="amount">
                            </TextBox>
                        </DataTemplate>

                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Label x:Name="Label" Content="Layers" Margin="282,0,283,124" FontSize="48" BorderBrush="#FF4A5759" Background="#00000000" HorizontalContentAlignment="Center" FontFamily="Cooper Black" FontStyle="Normal" Foreground="#FF4A5759" FontWeight="Bold" RenderTransformOrigin="0.505,0.149"/>


        <materialDesign:ColorZone 
Mode="PrimaryLight"
Grid.Row="0"
Grid.Column="0"
            Background="#FFDEDBD2"
Padding="16" Height="73" Margin="0,0,722,0">
            <StackPanel
    Orientation="Horizontal">
                <ToggleButton

        Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}"
        CommandParameter="{x:Static Dock.Left}"

        Style="{DynamicResource MaterialDesignHamburgerToggleButton}" Height="29" Width="29"/>
                <TextBlock
        VerticalAlignment="Center"
        Margin="16 0 0 0"><Run Text="Material Design In XAML Toolkit"/></TextBlock>
            </StackPanel>
        </materialDesign:ColorZone>




        <materialDesign:PackIcon Kind="Cart" Height="32" Width="118" Margin="682,10,0,0" MouseDoubleClick="ViewCart" IsEnabled="True"/>
        <materialDesign:PackIcon Kind="Package" Height="34" Width="76" Margin="648,8,0,0" MouseDoubleClick="Track_Order" IsEnabled="True"/>

    </Grid>

</Window>
